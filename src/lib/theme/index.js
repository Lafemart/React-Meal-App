import { colors } from "./Colors";
import { fonts, fontWeights, fontSizes, borderRadius } from "./fonts";
import { lineHeights, indents } from "./spacing";

export {
    fonts,
    colors, 
    indents,
    fontSizes, 
    lineHeights, 
    fontWeights, 
    borderRadius,
}